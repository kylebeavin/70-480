$(document).ready(function () {
    $('table,thead,tbody,th,tr,td').css('border', '1px solid black').css('padding', '10px');
    // .each(function(index,Element)) is used to iterate through jQuery objects:
    $('td').each(function (index) {
    });
});